<% if @product %>
  <%= @product.taxon.taxonomy.title %>
  <% @product.taxon.root.self_and_descendants.each do |taxon| %>
   |  <%= taxon.title %> 
  <% end %>
<% else %>
  <%= @taxon.taxonomy.title %>
  <% @taxon.root.self_and_descendants.each do |taxon| %>
   |  <%= taxon.title %> 
  <% end %>
<% end %>