<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	<div class="row">
		<div class="span4"><h2>Комментарий к заказу</h2><br />
	    <%= f.text_field :comment, :placeholder => "Ваше пожелания...", :class => 'form-input' %></div>
		<div class="span5 offset3"><%= link_to "Оформить заказ", "#", :class => 'order_button', :onclick=>"$('#new_order').submit();return false;" %></div>
	</div>
<% end %>
